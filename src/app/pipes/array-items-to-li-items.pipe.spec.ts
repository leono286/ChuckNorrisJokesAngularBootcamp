import { ArrayItemsToLiItemsPipe } from './array-items-to-li-items.pipe';

describe('ArrayItemsToLiItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayItemsToLiItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
